# alx-system_engineering-devops
# Shell, init files, variables and expansions

This project contains scripts that introduce shell initialization files,
variables, expansions, and alias creation.

## Files
0-alias — Creates an alias `ls` that runs `rm *`
